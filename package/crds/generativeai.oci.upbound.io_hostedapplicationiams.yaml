---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: hostedapplicationiams.generativeai.oci.upbound.io
spec:
  group: generativeai.oci.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - oci
    kind: HostedApplicationIam
    listKind: HostedApplicationIamList
    plural: hostedapplicationiams
    singular: hostedapplicationiam
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HostedApplicationIam is the Schema for the HostedApplicationIams
          API. Provides the Hosted Application IAM resource in Oracle Cloud Infrastructure
          Generative AI service
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HostedApplicationIamSpec defines the desired state of HostedApplicationIam
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  compartmentId:
                    description: (Updatable) The compartment OCID for the Hosted Application.
                    type: string
                  compartmentIdRef:
                    description: Reference to a Compartment in identity to populate
                      compartmentId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  compartmentIdSelector:
                    description: Selector for a Compartment in identity to populate
                      compartmentId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Defined tags for this resource. Each
                      key is predefined and scoped to a namespace. For more information,
                      see Resource Tags.  Example: {"Operations.CostCenter": "42"}'
                    type: object
                    x-kubernetes-map-type: granular
                  description:
                    description: (Updatable) The description for the Hosted Application.
                    type: string
                  displayName:
                    description: (Updatable) The user-friendly display name for the
                      Hosted Application. Does not need to be unique and can be updated
                      after creation.
                    type: string
                  environmentVariables:
                    description: (Updatable) The list of environment variables for
                      the Hosted Application. Defines a list of environment variables
                      injected at runtime.
                    items:
                      properties:
                        name:
                          description: (Updatable) Name of the environment variable.
                          type: string
                        type:
                          description: (Updatable) Type of the environment variable
                            (PLAINTEXT or VAULT, no default value).
                          type: string
                        value:
                          description: (Updatable) Value of the environment variable.
                          type: string
                      type: object
                    type: array
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Free-form tags for this resource. Each
                      tag is a simple key-value pair with no predefined name, type,
                      or namespace. For more information, see Resource Tags.  Example:
                      {"Department": "Finance"}'
                    type: object
                    x-kubernetes-map-type: granular
                  networkingConfig:
                    description: Networking configuration.
                    items:
                      properties:
                        inboundNetworkingConfig:
                          description: Inbound Networking configuration.
                          items:
                            properties:
                              endpointMode:
                                description: inbounding from public or private endpoint.
                                type: string
                              privateEndpointId:
                                description: The [OCID] of Private Endpoint when endpointMode=Private
                                type: string
                            type: object
                          type: array
                        outboundNetworkingConfig:
                          description: Outbound Networking configuration.
                          items:
                            properties:
                              customSubnetId:
                                description: ocid of customer subnet when networkMode=Custom
                                type: string
                              networkMode:
                                description: outbounding to managed internet or customer
                                  network.
                                type: string
                              nsgIds:
                                description: A list of the OCIDs of the network security
                                  groups that the private endpoint's VNIC belongs
                                  to.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                      type: object
                    type: array
                  scalingConfig:
                    description: (Updatable) The auto scaling configuration for the
                      Hosted Application. Defines the minimum and maximum number of
                      replicas. When unspecified, the service applies service-defined
                      default scaling values.
                    items:
                      properties:
                        maxReplica:
                          description: (Updatable) Maximum number of replicas allowed.
                          type: number
                        minReplica:
                          description: (Updatable) Minimum number of replicas to keep
                            running.
                          type: number
                        scalingType:
                          description: (Updatable) scaling type for application.
                          type: string
                        targetConcurrencyThreshold:
                          description: (Updatable) number of simultaneous requests
                            that can be processed by each replica.
                          type: number
                        targetCpuThreshold:
                          description: (Updatable) Scale up if average CPU utilization
                            exceeds this threshold.
                          type: number
                        targetMemoryThreshold:
                          description: (Updatable) Scale up if average memory utilization
                            exceeds this threshold.
                          type: number
                        targetRpsThreshold:
                          description: (Updatable) requests-per-second per replica
                            of an application.
                          type: number
                      type: object
                    type: array
                  storageConfigs:
                    description: The list of storage configuration for the Hosted
                      Application. Defines a list of service-managed storage back-ends.
                    items:
                      properties:
                        environmentVariableKey:
                          description: The key of environment variable to store the
                            database connection.
                          type: string
                        storageId:
                          description: The [OCID] of ApplicationStorage.
                          type: string
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  compartmentId:
                    description: (Updatable) The compartment OCID for the Hosted Application.
                    type: string
                  compartmentIdRef:
                    description: Reference to a Compartment in identity to populate
                      compartmentId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  compartmentIdSelector:
                    description: Selector for a Compartment in identity to populate
                      compartmentId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Defined tags for this resource. Each
                      key is predefined and scoped to a namespace. For more information,
                      see Resource Tags.  Example: {"Operations.CostCenter": "42"}'
                    type: object
                    x-kubernetes-map-type: granular
                  description:
                    description: (Updatable) The description for the Hosted Application.
                    type: string
                  displayName:
                    description: (Updatable) The user-friendly display name for the
                      Hosted Application. Does not need to be unique and can be updated
                      after creation.
                    type: string
                  environmentVariables:
                    description: (Updatable) The list of environment variables for
                      the Hosted Application. Defines a list of environment variables
                      injected at runtime.
                    items:
                      properties:
                        name:
                          description: (Updatable) Name of the environment variable.
                          type: string
                        type:
                          description: (Updatable) Type of the environment variable
                            (PLAINTEXT or VAULT, no default value).
                          type: string
                        value:
                          description: (Updatable) Value of the environment variable.
                          type: string
                      type: object
                    type: array
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Free-form tags for this resource. Each
                      tag is a simple key-value pair with no predefined name, type,
                      or namespace. For more information, see Resource Tags.  Example:
                      {"Department": "Finance"}'
                    type: object
                    x-kubernetes-map-type: granular
                  networkingConfig:
                    description: Networking configuration.
                    items:
                      properties:
                        inboundNetworkingConfig:
                          description: Inbound Networking configuration.
                          items:
                            properties:
                              endpointMode:
                                description: inbounding from public or private endpoint.
                                type: string
                              privateEndpointId:
                                description: The [OCID] of Private Endpoint when endpointMode=Private
                                type: string
                            type: object
                          type: array
                        outboundNetworkingConfig:
                          description: Outbound Networking configuration.
                          items:
                            properties:
                              customSubnetId:
                                description: ocid of customer subnet when networkMode=Custom
                                type: string
                              networkMode:
                                description: outbounding to managed internet or customer
                                  network.
                                type: string
                              nsgIds:
                                description: A list of the OCIDs of the network security
                                  groups that the private endpoint's VNIC belongs
                                  to.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                      type: object
                    type: array
                  scalingConfig:
                    description: (Updatable) The auto scaling configuration for the
                      Hosted Application. Defines the minimum and maximum number of
                      replicas. When unspecified, the service applies service-defined
                      default scaling values.
                    items:
                      properties:
                        maxReplica:
                          description: (Updatable) Maximum number of replicas allowed.
                          type: number
                        minReplica:
                          description: (Updatable) Minimum number of replicas to keep
                            running.
                          type: number
                        scalingType:
                          description: (Updatable) scaling type for application.
                          type: string
                        targetConcurrencyThreshold:
                          description: (Updatable) number of simultaneous requests
                            that can be processed by each replica.
                          type: number
                        targetCpuThreshold:
                          description: (Updatable) Scale up if average CPU utilization
                            exceeds this threshold.
                          type: number
                        targetMemoryThreshold:
                          description: (Updatable) Scale up if average memory utilization
                            exceeds this threshold.
                          type: number
                        targetRpsThreshold:
                          description: (Updatable) requests-per-second per replica
                            of an application.
                          type: number
                      type: object
                    type: array
                  storageConfigs:
                    description: The list of storage configuration for the Hosted
                      Application. Defines a list of service-managed storage back-ends.
                    items:
                      properties:
                        environmentVariableKey:
                          description: The key of environment variable to store the
                            database connection.
                          type: string
                        storageId:
                          description: The [OCID] of ApplicationStorage.
                          type: string
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  - MustCreate
                  - Orphan
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.displayName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.displayName)
                || (has(self.initProvider) && has(self.initProvider.displayName))'
          status:
            description: HostedApplicationIamStatus defines the observed state of
              HostedApplicationIam.
            properties:
              atProvider:
                properties:
                  compartmentId:
                    description: (Updatable) The compartment OCID for the Hosted Application.
                    type: string
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Defined tags for this resource. Each
                      key is predefined and scoped to a namespace. For more information,
                      see Resource Tags.  Example: {"Operations.CostCenter": "42"}'
                    type: object
                    x-kubernetes-map-type: granular
                  description:
                    description: (Updatable) The description for the Hosted Application.
                    type: string
                  displayName:
                    description: (Updatable) The user-friendly display name for the
                      Hosted Application. Does not need to be unique and can be updated
                      after creation.
                    type: string
                  environmentVariables:
                    description: (Updatable) The list of environment variables for
                      the Hosted Application. Defines a list of environment variables
                      injected at runtime.
                    items:
                      properties:
                        name:
                          description: (Updatable) Name of the environment variable.
                          type: string
                        type:
                          description: (Updatable) Type of the environment variable
                            (PLAINTEXT or VAULT, no default value).
                          type: string
                        value:
                          description: (Updatable) Value of the environment variable.
                          type: string
                      type: object
                    type: array
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Free-form tags for this resource. Each
                      tag is a simple key-value pair with no predefined name, type,
                      or namespace. For more information, see Resource Tags.  Example:
                      {"Department": "Finance"}'
                    type: object
                    x-kubernetes-map-type: granular
                  id:
                    description: The OCID of the hosted application.
                    type: string
                  lifecycleDetails:
                    description: A message describing the current state of the endpoint
                      in more detail that can provide actionable information.
                    type: string
                  networkingConfig:
                    description: Networking configuration.
                    items:
                      properties:
                        inboundNetworkingConfig:
                          description: Inbound Networking configuration.
                          items:
                            properties:
                              endpointMode:
                                description: inbounding from public or private endpoint.
                                type: string
                              privateEndpointId:
                                description: The [OCID] of Private Endpoint when endpointMode=Private
                                type: string
                            type: object
                          type: array
                        outboundNetworkingConfig:
                          description: Outbound Networking configuration.
                          items:
                            properties:
                              customSubnetId:
                                description: ocid of customer subnet when networkMode=Custom
                                type: string
                              networkMode:
                                description: outbounding to managed internet or customer
                                  network.
                                type: string
                              nsgIds:
                                description: A list of the OCIDs of the network security
                                  groups that the private endpoint's VNIC belongs
                                  to.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                      type: object
                    type: array
                  scalingConfig:
                    description: (Updatable) The auto scaling configuration for the
                      Hosted Application. Defines the minimum and maximum number of
                      replicas. When unspecified, the service applies service-defined
                      default scaling values.
                    items:
                      properties:
                        maxReplica:
                          description: (Updatable) Maximum number of replicas allowed.
                          type: number
                        minReplica:
                          description: (Updatable) Minimum number of replicas to keep
                            running.
                          type: number
                        scalingType:
                          description: (Updatable) scaling type for application.
                          type: string
                        targetConcurrencyThreshold:
                          description: (Updatable) number of simultaneous requests
                            that can be processed by each replica.
                          type: number
                        targetCpuThreshold:
                          description: (Updatable) Scale up if average CPU utilization
                            exceeds this threshold.
                          type: number
                        targetMemoryThreshold:
                          description: (Updatable) Scale up if average memory utilization
                            exceeds this threshold.
                          type: number
                        targetRpsThreshold:
                          description: (Updatable) requests-per-second per replica
                            of an application.
                          type: number
                      type: object
                    type: array
                  state:
                    description: The current state of the hosted application.
                    type: string
                  storageConfigs:
                    description: The list of storage configuration for the Hosted
                      Application. Defines a list of service-managed storage back-ends.
                    items:
                      properties:
                        environmentVariableKey:
                          description: The key of environment variable to store the
                            database connection.
                          type: string
                        storageId:
                          description: The [OCID] of ApplicationStorage.
                          type: string
                      type: object
                    type: array
                  systemTags:
                    additionalProperties:
                      type: string
                    description: 'System tags for this resource. Each key is predefined
                      and scoped to a namespace.  Example: {"orcl-cloud.free-tier-retained":
                      "true"}'
                    type: object
                    x-kubernetes-map-type: granular
                  timeCreated:
                    description: The date and time the hosted application was created,
                      in the format defined by RFC 3339
                    type: string
                  timeUpdated:
                    description: The date and time the hosted application was updated,
                      in the format defined by RFC 3339
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
