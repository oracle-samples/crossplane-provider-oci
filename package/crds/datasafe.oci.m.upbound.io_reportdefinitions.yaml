---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: reportdefinitions.datasafe.oci.m.upbound.io
spec:
  group: datasafe.oci.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - oci
    kind: ReportDefinition
    listKind: ReportDefinitionList
    plural: reportdefinitions
    singular: reportdefinition
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ReportDefinition is the Schema for the ReportDefinitions API.
          Provides the Report Definition resource in Oracle Cloud Infrastructure Data
          Safe service
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ReportDefinitionSpec defines the desired state of ReportDefinition
            properties:
              forProvider:
                properties:
                  columnFilters:
                    description: (Updatable) An array of column filter objects. A
                      column Filter object stores all information about a column filter
                      including field name, an operator, one or more expressions,
                      if the filter is enabled, or if the filter is hidden.
                    items:
                      properties:
                        expressions:
                          description: (Updatable) An array of expressions based on
                            the operator type. A filter may have one or more expressions.
                          items:
                            type: string
                          type: array
                        fieldName:
                          description: (Updatable) Name of the column on which the
                            filter must be applied.
                          type: string
                        isEnabled:
                          description: (Updatable) Indicates whether the filter is
                            enabled. Values can either be 'true' or 'false'.
                          type: boolean
                        isHidden:
                          description: (Updatable) Indicates whether the filter is
                            hidden. Values can either be 'true' or 'false'.
                          type: boolean
                        operator:
                          description: (Updatable) Specifies the type of operator
                            that must be applied for example in, eq etc.
                          type: string
                      type: object
                    type: array
                  columnInfo:
                    description: (Updatable) An array of column objects in the order
                      (left to right) displayed in the report. A column object stores
                      all information about a column, including the name displayed
                      on the UI, corresponding field name in the data source, data
                      type of the column, and column visibility (if the column is
                      visible to the user).
                    items:
                      properties:
                        applicableOperators:
                          description: (Updatable) An array of operators that can
                            be supported by column fieldName.
                          items:
                            type: string
                          type: array
                        dataType:
                          description: (Updatable) Specifies the data type of the
                            column.
                          type: string
                        displayName:
                          description: (Updatable) Name of the column displayed on
                            UI.
                          type: string
                        displayOrder:
                          description: (Updatable) Specifies the display order of
                            the column.
                          type: number
                        fieldName:
                          description: (Updatable) Name of the column on which the
                            filter must be applied.
                          type: string
                        isHidden:
                          description: (Updatable) Indicates whether the filter is
                            hidden. Values can either be 'true' or 'false'.
                          type: boolean
                        isVirtual:
                          description: (Updatable) Specifies if column is virtual
                            and can only be used as column filter.
                          type: boolean
                      type: object
                    type: array
                  columnSortings:
                    description: (Updatable) An array of column sorting objects. Each
                      column sorting object stores the column name to be sorted and
                      if the sorting is in ascending order; sorting is done by the
                      first column in the array, then by the second column in the
                      array, etc.
                    items:
                      properties:
                        fieldName:
                          description: (Updatable) Name of the column on which the
                            filter must be applied.
                          type: string
                        isAscending:
                          description: (Updatable) Indicates if the column must be
                            sorted in ascending order. Values can either be 'true'
                            or 'false'.
                          type: boolean
                        sortingOrder:
                          description: (Updatable) Indicates the order at which column
                            must be sorted.
                          type: number
                      type: object
                    type: array
                  compartmentId:
                    description: (Updatable) The OCID of the compartment containing
                      the report definition.
                    type: string
                  compartmentIdRef:
                    description: Reference to a Compartment in identity to populate
                      compartmentId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  compartmentIdSelector:
                    description: Selector for a Compartment in identity to populate
                      compartmentId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Defined tags for this resource. Each
                      key is predefined and scoped to a namespace. For more information,
                      see Resource Tags Example: {"Operations.CostCenter": "42"}'
                    type: object
                    x-kubernetes-map-type: granular
                  description:
                    description: (Updatable) The description of the report definition.
                    type: string
                  displayName:
                    description: (Updatable) Name of the column displayed on UI.
                    type: string
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Free-form tags for this resource. Each
                      tag is a simple key-value pair with no predefined name, type,
                      or namespace. For more information, see Resource Tags  Example:
                      {"Department": "Finance"}'
                    type: object
                    x-kubernetes-map-type: granular
                  parentId:
                    description: The OCID of the parent report definition.
                    type: string
                  summary:
                    description: (Updatable) An array of report summary objects in
                      the order (left to right)  displayed in the report.  A  report
                      summary object stores all information about summary of report
                      to be displayed, including the name displayed on UI, the display
                      order, corresponding group by and count of values, summary visibility
                      (if the summary is visible to user).
                    items:
                      properties:
                        countOf:
                          description: (Updatable) Name of the key or count of object.
                          type: string
                        displayOrder:
                          description: (Updatable) Specifies the display order of
                            the column.
                          type: number
                        groupByFieldName:
                          description: (Updatable) A comma-delimited string that specifies
                            the names of the fields by which the records must be aggregated
                            to get the summary.
                          type: string
                        isHidden:
                          description: (Updatable) Indicates whether the filter is
                            hidden. Values can either be 'true' or 'false'.
                          type: boolean
                        name:
                          description: (Updatable) Name of the report summary.
                          type: string
                        scimFilter:
                          description: (Updatable) Additional scim filters used to
                            get the specific summary.
                          type: string
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  columnFilters:
                    description: (Updatable) An array of column filter objects. A
                      column Filter object stores all information about a column filter
                      including field name, an operator, one or more expressions,
                      if the filter is enabled, or if the filter is hidden.
                    items:
                      properties:
                        expressions:
                          description: (Updatable) An array of expressions based on
                            the operator type. A filter may have one or more expressions.
                          items:
                            type: string
                          type: array
                        fieldName:
                          description: (Updatable) Name of the column on which the
                            filter must be applied.
                          type: string
                        isEnabled:
                          description: (Updatable) Indicates whether the filter is
                            enabled. Values can either be 'true' or 'false'.
                          type: boolean
                        isHidden:
                          description: (Updatable) Indicates whether the filter is
                            hidden. Values can either be 'true' or 'false'.
                          type: boolean
                        operator:
                          description: (Updatable) Specifies the type of operator
                            that must be applied for example in, eq etc.
                          type: string
                      type: object
                    type: array
                  columnInfo:
                    description: (Updatable) An array of column objects in the order
                      (left to right) displayed in the report. A column object stores
                      all information about a column, including the name displayed
                      on the UI, corresponding field name in the data source, data
                      type of the column, and column visibility (if the column is
                      visible to the user).
                    items:
                      properties:
                        applicableOperators:
                          description: (Updatable) An array of operators that can
                            be supported by column fieldName.
                          items:
                            type: string
                          type: array
                        dataType:
                          description: (Updatable) Specifies the data type of the
                            column.
                          type: string
                        displayName:
                          description: (Updatable) Name of the column displayed on
                            UI.
                          type: string
                        displayOrder:
                          description: (Updatable) Specifies the display order of
                            the column.
                          type: number
                        fieldName:
                          description: (Updatable) Name of the column on which the
                            filter must be applied.
                          type: string
                        isHidden:
                          description: (Updatable) Indicates whether the filter is
                            hidden. Values can either be 'true' or 'false'.
                          type: boolean
                        isVirtual:
                          description: (Updatable) Specifies if column is virtual
                            and can only be used as column filter.
                          type: boolean
                      type: object
                    type: array
                  columnSortings:
                    description: (Updatable) An array of column sorting objects. Each
                      column sorting object stores the column name to be sorted and
                      if the sorting is in ascending order; sorting is done by the
                      first column in the array, then by the second column in the
                      array, etc.
                    items:
                      properties:
                        fieldName:
                          description: (Updatable) Name of the column on which the
                            filter must be applied.
                          type: string
                        isAscending:
                          description: (Updatable) Indicates if the column must be
                            sorted in ascending order. Values can either be 'true'
                            or 'false'.
                          type: boolean
                        sortingOrder:
                          description: (Updatable) Indicates the order at which column
                            must be sorted.
                          type: number
                      type: object
                    type: array
                  compartmentId:
                    description: (Updatable) The OCID of the compartment containing
                      the report definition.
                    type: string
                  compartmentIdRef:
                    description: Reference to a Compartment in identity to populate
                      compartmentId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  compartmentIdSelector:
                    description: Selector for a Compartment in identity to populate
                      compartmentId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Defined tags for this resource. Each
                      key is predefined and scoped to a namespace. For more information,
                      see Resource Tags Example: {"Operations.CostCenter": "42"}'
                    type: object
                    x-kubernetes-map-type: granular
                  description:
                    description: (Updatable) The description of the report definition.
                    type: string
                  displayName:
                    description: (Updatable) Name of the column displayed on UI.
                    type: string
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Free-form tags for this resource. Each
                      tag is a simple key-value pair with no predefined name, type,
                      or namespace. For more information, see Resource Tags  Example:
                      {"Department": "Finance"}'
                    type: object
                    x-kubernetes-map-type: granular
                  parentId:
                    description: The OCID of the parent report definition.
                    type: string
                  summary:
                    description: (Updatable) An array of report summary objects in
                      the order (left to right)  displayed in the report.  A  report
                      summary object stores all information about summary of report
                      to be displayed, including the name displayed on UI, the display
                      order, corresponding group by and count of values, summary visibility
                      (if the summary is visible to user).
                    items:
                      properties:
                        countOf:
                          description: (Updatable) Name of the key or count of object.
                          type: string
                        displayOrder:
                          description: (Updatable) Specifies the display order of
                            the column.
                          type: number
                        groupByFieldName:
                          description: (Updatable) A comma-delimited string that specifies
                            the names of the fields by which the records must be aggregated
                            to get the summary.
                          type: string
                        isHidden:
                          description: (Updatable) Indicates whether the filter is
                            hidden. Values can either be 'true' or 'false'.
                          type: boolean
                        name:
                          description: (Updatable) Name of the report summary.
                          type: string
                        scimFilter:
                          description: (Updatable) Additional scim filters used to
                            get the specific summary.
                          type: string
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  - MustCreate
                  - Orphan
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.columnFilters is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.columnFilters)
                || (has(self.initProvider) && has(self.initProvider.columnFilters))'
            - message: spec.forProvider.columnInfo is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.columnInfo)
                || (has(self.initProvider) && has(self.initProvider.columnInfo))'
            - message: spec.forProvider.columnSortings is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.columnSortings)
                || (has(self.initProvider) && has(self.initProvider.columnSortings))'
            - message: spec.forProvider.displayName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.displayName)
                || (has(self.initProvider) && has(self.initProvider.displayName))'
            - message: spec.forProvider.parentId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.parentId)
                || (has(self.initProvider) && has(self.initProvider.parentId))'
            - message: spec.forProvider.summary is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.summary)
                || (has(self.initProvider) && has(self.initProvider.summary))'
          status:
            description: ReportDefinitionStatus defines the observed state of ReportDefinition.
            properties:
              atProvider:
                properties:
                  category:
                    description: Specifies the name of the category that this report
                      belongs to.
                    type: string
                  columnFilters:
                    description: (Updatable) An array of column filter objects. A
                      column Filter object stores all information about a column filter
                      including field name, an operator, one or more expressions,
                      if the filter is enabled, or if the filter is hidden.
                    items:
                      properties:
                        expressions:
                          description: (Updatable) An array of expressions based on
                            the operator type. A filter may have one or more expressions.
                          items:
                            type: string
                          type: array
                        fieldName:
                          description: (Updatable) Name of the column on which the
                            filter must be applied.
                          type: string
                        isEnabled:
                          description: (Updatable) Indicates whether the filter is
                            enabled. Values can either be 'true' or 'false'.
                          type: boolean
                        isHidden:
                          description: (Updatable) Indicates whether the filter is
                            hidden. Values can either be 'true' or 'false'.
                          type: boolean
                        operator:
                          description: (Updatable) Specifies the type of operator
                            that must be applied for example in, eq etc.
                          type: string
                      type: object
                    type: array
                  columnInfo:
                    description: (Updatable) An array of column objects in the order
                      (left to right) displayed in the report. A column object stores
                      all information about a column, including the name displayed
                      on the UI, corresponding field name in the data source, data
                      type of the column, and column visibility (if the column is
                      visible to the user).
                    items:
                      properties:
                        applicableOperators:
                          description: (Updatable) An array of operators that can
                            be supported by column fieldName.
                          items:
                            type: string
                          type: array
                        dataType:
                          description: (Updatable) Specifies the data type of the
                            column.
                          type: string
                        displayName:
                          description: (Updatable) Name of the column displayed on
                            UI.
                          type: string
                        displayOrder:
                          description: (Updatable) Specifies the display order of
                            the column.
                          type: number
                        fieldName:
                          description: (Updatable) Name of the column on which the
                            filter must be applied.
                          type: string
                        isHidden:
                          description: (Updatable) Indicates whether the filter is
                            hidden. Values can either be 'true' or 'false'.
                          type: boolean
                        isVirtual:
                          description: (Updatable) Specifies if column is virtual
                            and can only be used as column filter.
                          type: boolean
                      type: object
                    type: array
                  columnSortings:
                    description: (Updatable) An array of column sorting objects. Each
                      column sorting object stores the column name to be sorted and
                      if the sorting is in ascending order; sorting is done by the
                      first column in the array, then by the second column in the
                      array, etc.
                    items:
                      properties:
                        fieldName:
                          description: (Updatable) Name of the column on which the
                            filter must be applied.
                          type: string
                        isAscending:
                          description: (Updatable) Indicates if the column must be
                            sorted in ascending order. Values can either be 'true'
                            or 'false'.
                          type: boolean
                        sortingOrder:
                          description: (Updatable) Indicates the order at which column
                            must be sorted.
                          type: number
                      type: object
                    type: array
                  compartmentId:
                    description: (Updatable) The OCID of the compartment containing
                      the report definition.
                    type: string
                  complianceStandards:
                    description: The list of the data protection regulations/standards
                      used in the report that will help demonstrate compliance.
                    items:
                      type: string
                    type: array
                  dataSource:
                    description: Specifies the name of a resource that provides data
                      for the report. For example alerts, events.
                    type: string
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Defined tags for this resource. Each
                      key is predefined and scoped to a namespace. For more information,
                      see Resource Tags Example: {"Operations.CostCenter": "42"}'
                    type: object
                    x-kubernetes-map-type: granular
                  description:
                    description: (Updatable) The description of the report definition.
                    type: string
                  displayName:
                    description: (Updatable) Name of the column displayed on UI.
                    type: string
                  displayOrder:
                    description: (Updatable) Specifies the display order of the column.
                    type: number
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Free-form tags for this resource. Each
                      tag is a simple key-value pair with no predefined name, type,
                      or namespace. For more information, see Resource Tags  Example:
                      {"Department": "Finance"}'
                    type: object
                    x-kubernetes-map-type: granular
                  id:
                    description: The OCID of the report definition.
                    type: string
                  isSchedulePaginationEnabled:
                    description: Indicates if the reports being generated should be
                      paginated. If set to true, multiple reports can be generated
                      and the details of next and previous report are present in Report.
                      Values can either be 'true' or 'false'.
                    type: boolean
                  isSeeded:
                    description: Signifies whether the definition is seeded or user
                      defined. Values can either be 'true' or 'false'.
                    type: boolean
                  lifecycleDetails:
                    description: Details about the current state of the report definition
                      in Data Safe.
                    type: string
                  parentId:
                    description: The OCID of the parent report definition.
                    type: string
                  recordTimeSpan:
                    description: 'The time span for the records in the report to be
                      scheduled.  Allowed period strings - "H","D","M","Y" Each of
                      the above fields potentially introduce constraints. A workRequest
                      is created only when period-value satisfies all the constraints.
                      Constraints introduced: 1. period = H (The allowed range for
                      period-value is [1, 23]) 2. period = D (The allowed range for
                      period-value is [1, 30]) 3. period = M (The allowed range for
                      period-value is [1, 11]) 4. period = Y (The minimum period-value
                      is 1)'
                    type: string
                  schedule:
                    description: 'The schedule to generate the report periodically
                      in the specified format: ;'
                    type: string
                  scheduledReportCompartmentId:
                    description: The OCID of the compartment in which the scheduled
                      resource will be created.
                    type: string
                  scheduledReportMimeType:
                    description: Specifies the format of the report ( either .xls
                      or .pdf or .json)
                    type: string
                  scheduledReportName:
                    description: The name of the report to be scheduled.
                    type: string
                  scheduledReportRowLimit:
                    description: Specifies the limit on the number of rows in the
                      report.
                    type: number
                  scimFilter:
                    description: (Updatable) Additional scim filters used to get the
                      specific summary.
                    type: string
                  state:
                    description: The current state of the report.
                    type: string
                  summary:
                    description: (Updatable) An array of report summary objects in
                      the order (left to right)  displayed in the report.  A  report
                      summary object stores all information about summary of report
                      to be displayed, including the name displayed on UI, the display
                      order, corresponding group by and count of values, summary visibility
                      (if the summary is visible to user).
                    items:
                      properties:
                        countOf:
                          description: (Updatable) Name of the key or count of object.
                          type: string
                        displayOrder:
                          description: (Updatable) Specifies the display order of
                            the column.
                          type: number
                        groupByFieldName:
                          description: (Updatable) A comma-delimited string that specifies
                            the names of the fields by which the records must be aggregated
                            to get the summary.
                          type: string
                        isHidden:
                          description: (Updatable) Indicates whether the filter is
                            hidden. Values can either be 'true' or 'false'.
                          type: boolean
                        name:
                          description: (Updatable) Name of the report summary.
                          type: string
                        scimFilter:
                          description: (Updatable) Additional scim filters used to
                            get the specific summary.
                          type: string
                      type: object
                    type: array
                  systemTags:
                    additionalProperties:
                      type: string
                    description: 'System tags for this resource. Each key is predefined
                      and scoped to a namespace. For more information, see Resource
                      Tags. Example: {"orcl-cloud.free-tier-retained": "true"}'
                    type: object
                    x-kubernetes-map-type: granular
                  timeCreated:
                    description: Specifies the date and time the report definition
                      was created.
                    type: string
                  timeUpdated:
                    description: The date and time the report definition was updated.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
