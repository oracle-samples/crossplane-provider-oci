---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: schedules.resourcescheduler.oci.m.upbound.io
spec:
  group: resourcescheduler.oci.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - oci
    kind: Schedule
    listKind: ScheduleList
    plural: schedules
    singular: schedule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Schedule is the Schema for the Schedules API. Provides the Schedule
          resource in Oracle Cloud Infrastructure Resource Scheduler service
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScheduleSpec defines the desired state of Schedule
            properties:
              forProvider:
                properties:
                  action:
                    description: (Updatable) This is the action that will be executed
                      by the schedule.
                    type: string
                  compartmentId:
                    description: (Updatable) The OCID of the compartment in which
                      the schedule is created
                    type: string
                  compartmentIdRef:
                    description: Reference to a Compartment in identity to populate
                      compartmentId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  compartmentIdSelector:
                    description: Selector for a Compartment in identity to populate
                      compartmentId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) These are defined tags for this resource.
                      Each key is predefined and scoped to a namespace. For more information,
                      see Resource Tags.  Example: {"Operations.CostCenter": "42"}'
                    type: object
                    x-kubernetes-map-type: granular
                  description:
                    description: (Updatable) This is the description of the schedule.
                    type: string
                  displayName:
                    description: (Updatable) This is a user-friendly name for the
                      schedule. It does not have to be unique, and it's changeable.
                    type: string
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) These are free-form tags for this resource.
                      Each tag is a simple key-value pair with no predefined name,
                      type, or namespace. For more information, see Resource Tags.  Example:
                      {"Department": "Finance"}'
                    type: object
                    x-kubernetes-map-type: granular
                  localTimeZone:
                    description: (Updatable) IANA timezone identifier (e.g., 'America/New_York',
                      'UTC', 'Europe/London'). This determines the timezone context
                      for evaluating the recurrence expression.
                    type: string
                  recurrenceDetails:
                    description: (Updatable) This is the frequency of recurrence of
                      a schedule. The frequency field can either conform to RFC-5545
                      formatting or UNIX cron formatting for recurrences, based on
                      the value specified by the recurrenceType field.
                    type: string
                  recurrenceType:
                    description: (Updatable) Type of recurrence of a schedule
                    type: string
                  resourceFilters:
                    description: (Updatable) This is a list of resources filters.  The
                      schedule will be applied to resources matching all of them.
                    items:
                      properties:
                        attribute:
                          description: '(Updatable) This is the resource attribute
                            on which the threshold is defined. We support 5 different
                            types of attributes: DEFINED_TAGS, COMPARTMENT_ID, TIME_CREATED,
                            LIFECYCLE_STATE and RESOURCE_TYPE.'
                          type: string
                        condition:
                          description: (Applicable when attribute=TIME_CREATED) (Updatable)
                            This is the condition for the filter in comparison to
                            its creation time. Could be set to EQUAL, BEFORE and AFTER.
                          type: string
                        shouldIncludeChildCompartments:
                          description: (Updatable) This boolean sets whether to include
                            child compartments.
                          type: boolean
                        value:
                          description: '(Updatable) This is a collection of resource
                            filter values, different types of filter has different
                            value format, see below:'
                          items:
                            properties:
                              namespace:
                                description: (Updatable) This is the namespace of
                                  the defined tag.
                                type: string
                              tagKey:
                                description: (Updatable) This is the key of the defined
                                  tag.
                                type: string
                              value:
                                description: '(Updatable) This is a collection of
                                  resource filter values, different types of filter
                                  has different value format, see below:'
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  resources:
                    description: (Updatable) This is the list of resources to which
                      the scheduled operation is applied.
                    items:
                      properties:
                        id:
                          description: (Updatable) This is the resource OCID.
                          type: string
                        metadata:
                          additionalProperties:
                            type: string
                          description: |-
                            (Updatable) This is additional information that helps to identity the resource for the schedule.
                            { "id": "<OCID_of_bucket>" "metadata": { "namespaceName": "sampleNamespace", "bucketName": "sampleBucket" } }
                          type: object
                          x-kubernetes-map-type: granular
                        parameters:
                          description: (Updatable) This is the user input parameters
                            to use when acting on the resource.
                          items:
                            properties:
                              parameterType:
                                description: (Updatable) This is the parameter type
                                  on which the input parameter is defined
                                type: string
                              value:
                                description: '(Updatable) This is a collection of
                                  resource filter values, different types of filter
                                  has different value format, see below:'
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  state:
                    description: (Updatable) The target state for the Schedule. Could
                      be set to ACTIVE or INACTIVE.
                    type: string
                  timeEnds:
                    description: '(Updatable) This is the date and time the schedule
                      ends, in the format defined by RFC 3339  Example: 2016-08-25T21:10:29.600Z'
                    type: string
                  timeStarts:
                    description: '(Updatable) This is the date and time the schedule
                      starts, in the format defined by RFC 3339  Example: 2016-08-25T21:10:29.600Z'
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  action:
                    description: (Updatable) This is the action that will be executed
                      by the schedule.
                    type: string
                  compartmentId:
                    description: (Updatable) The OCID of the compartment in which
                      the schedule is created
                    type: string
                  compartmentIdRef:
                    description: Reference to a Compartment in identity to populate
                      compartmentId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  compartmentIdSelector:
                    description: Selector for a Compartment in identity to populate
                      compartmentId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) These are defined tags for this resource.
                      Each key is predefined and scoped to a namespace. For more information,
                      see Resource Tags.  Example: {"Operations.CostCenter": "42"}'
                    type: object
                    x-kubernetes-map-type: granular
                  description:
                    description: (Updatable) This is the description of the schedule.
                    type: string
                  displayName:
                    description: (Updatable) This is a user-friendly name for the
                      schedule. It does not have to be unique, and it's changeable.
                    type: string
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) These are free-form tags for this resource.
                      Each tag is a simple key-value pair with no predefined name,
                      type, or namespace. For more information, see Resource Tags.  Example:
                      {"Department": "Finance"}'
                    type: object
                    x-kubernetes-map-type: granular
                  localTimeZone:
                    description: (Updatable) IANA timezone identifier (e.g., 'America/New_York',
                      'UTC', 'Europe/London'). This determines the timezone context
                      for evaluating the recurrence expression.
                    type: string
                  recurrenceDetails:
                    description: (Updatable) This is the frequency of recurrence of
                      a schedule. The frequency field can either conform to RFC-5545
                      formatting or UNIX cron formatting for recurrences, based on
                      the value specified by the recurrenceType field.
                    type: string
                  recurrenceType:
                    description: (Updatable) Type of recurrence of a schedule
                    type: string
                  resourceFilters:
                    description: (Updatable) This is a list of resources filters.  The
                      schedule will be applied to resources matching all of them.
                    items:
                      properties:
                        attribute:
                          description: '(Updatable) This is the resource attribute
                            on which the threshold is defined. We support 5 different
                            types of attributes: DEFINED_TAGS, COMPARTMENT_ID, TIME_CREATED,
                            LIFECYCLE_STATE and RESOURCE_TYPE.'
                          type: string
                        condition:
                          description: (Applicable when attribute=TIME_CREATED) (Updatable)
                            This is the condition for the filter in comparison to
                            its creation time. Could be set to EQUAL, BEFORE and AFTER.
                          type: string
                        shouldIncludeChildCompartments:
                          description: (Updatable) This boolean sets whether to include
                            child compartments.
                          type: boolean
                        value:
                          description: '(Updatable) This is a collection of resource
                            filter values, different types of filter has different
                            value format, see below:'
                          items:
                            properties:
                              namespace:
                                description: (Updatable) This is the namespace of
                                  the defined tag.
                                type: string
                              tagKey:
                                description: (Updatable) This is the key of the defined
                                  tag.
                                type: string
                              value:
                                description: '(Updatable) This is a collection of
                                  resource filter values, different types of filter
                                  has different value format, see below:'
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  resources:
                    description: (Updatable) This is the list of resources to which
                      the scheduled operation is applied.
                    items:
                      properties:
                        id:
                          description: (Updatable) This is the resource OCID.
                          type: string
                        metadata:
                          additionalProperties:
                            type: string
                          description: |-
                            (Updatable) This is additional information that helps to identity the resource for the schedule.
                            { "id": "<OCID_of_bucket>" "metadata": { "namespaceName": "sampleNamespace", "bucketName": "sampleBucket" } }
                          type: object
                          x-kubernetes-map-type: granular
                        parameters:
                          description: (Updatable) This is the user input parameters
                            to use when acting on the resource.
                          items:
                            properties:
                              parameterType:
                                description: (Updatable) This is the parameter type
                                  on which the input parameter is defined
                                type: string
                              value:
                                description: '(Updatable) This is a collection of
                                  resource filter values, different types of filter
                                  has different value format, see below:'
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  state:
                    description: (Updatable) The target state for the Schedule. Could
                      be set to ACTIVE or INACTIVE.
                    type: string
                  timeEnds:
                    description: '(Updatable) This is the date and time the schedule
                      ends, in the format defined by RFC 3339  Example: 2016-08-25T21:10:29.600Z'
                    type: string
                  timeStarts:
                    description: '(Updatable) This is the date and time the schedule
                      starts, in the format defined by RFC 3339  Example: 2016-08-25T21:10:29.600Z'
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  - MustCreate
                  - Orphan
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.action is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.action)
                || (has(self.initProvider) && has(self.initProvider.action))'
            - message: spec.forProvider.recurrenceDetails is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.recurrenceDetails)
                || (has(self.initProvider) && has(self.initProvider.recurrenceDetails))'
            - message: spec.forProvider.recurrenceType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.recurrenceType)
                || (has(self.initProvider) && has(self.initProvider.recurrenceType))'
          status:
            description: ScheduleStatus defines the observed state of Schedule.
            properties:
              atProvider:
                properties:
                  action:
                    description: (Updatable) This is the action that will be executed
                      by the schedule.
                    type: string
                  compartmentId:
                    description: (Updatable) The OCID of the compartment in which
                      the schedule is created
                    type: string
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) These are defined tags for this resource.
                      Each key is predefined and scoped to a namespace. For more information,
                      see Resource Tags.  Example: {"Operations.CostCenter": "42"}'
                    type: object
                    x-kubernetes-map-type: granular
                  description:
                    description: (Updatable) This is the description of the schedule.
                    type: string
                  displayName:
                    description: (Updatable) This is a user-friendly name for the
                      schedule. It does not have to be unique, and it's changeable.
                    type: string
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) These are free-form tags for this resource.
                      Each tag is a simple key-value pair with no predefined name,
                      type, or namespace. For more information, see Resource Tags.  Example:
                      {"Department": "Finance"}'
                    type: object
                    x-kubernetes-map-type: granular
                  id:
                    description: (Updatable) This is the resource OCID.
                    type: string
                  lastRunStatus:
                    description: This is the status of the last work request.
                    type: string
                  localTimeZone:
                    description: (Updatable) IANA timezone identifier (e.g., 'America/New_York',
                      'UTC', 'Europe/London'). This determines the timezone context
                      for evaluating the recurrence expression.
                    type: string
                  recurrenceDetails:
                    description: (Updatable) This is the frequency of recurrence of
                      a schedule. The frequency field can either conform to RFC-5545
                      formatting or UNIX cron formatting for recurrences, based on
                      the value specified by the recurrenceType field.
                    type: string
                  recurrenceType:
                    description: (Updatable) Type of recurrence of a schedule
                    type: string
                  resourceFilters:
                    description: (Updatable) This is a list of resources filters.  The
                      schedule will be applied to resources matching all of them.
                    items:
                      properties:
                        attribute:
                          description: '(Updatable) This is the resource attribute
                            on which the threshold is defined. We support 5 different
                            types of attributes: DEFINED_TAGS, COMPARTMENT_ID, TIME_CREATED,
                            LIFECYCLE_STATE and RESOURCE_TYPE.'
                          type: string
                        condition:
                          description: (Applicable when attribute=TIME_CREATED) (Updatable)
                            This is the condition for the filter in comparison to
                            its creation time. Could be set to EQUAL, BEFORE and AFTER.
                          type: string
                        shouldIncludeChildCompartments:
                          description: (Updatable) This boolean sets whether to include
                            child compartments.
                          type: boolean
                        value:
                          description: '(Updatable) This is a collection of resource
                            filter values, different types of filter has different
                            value format, see below:'
                          items:
                            properties:
                              namespace:
                                description: (Updatable) This is the namespace of
                                  the defined tag.
                                type: string
                              tagKey:
                                description: (Updatable) This is the key of the defined
                                  tag.
                                type: string
                              value:
                                description: '(Updatable) This is a collection of
                                  resource filter values, different types of filter
                                  has different value format, see below:'
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  resources:
                    description: (Updatable) This is the list of resources to which
                      the scheduled operation is applied.
                    items:
                      properties:
                        id:
                          description: (Updatable) This is the resource OCID.
                          type: string
                        metadata:
                          additionalProperties:
                            type: string
                          description: |-
                            (Updatable) This is additional information that helps to identity the resource for the schedule.
                            { "id": "<OCID_of_bucket>" "metadata": { "namespaceName": "sampleNamespace", "bucketName": "sampleBucket" } }
                          type: object
                          x-kubernetes-map-type: granular
                        parameters:
                          description: (Updatable) This is the user input parameters
                            to use when acting on the resource.
                          items:
                            properties:
                              parameterType:
                                description: (Updatable) This is the parameter type
                                  on which the input parameter is defined
                                type: string
                              value:
                                description: '(Updatable) This is a collection of
                                  resource filter values, different types of filter
                                  has different value format, see below:'
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  state:
                    description: (Updatable) The target state for the Schedule. Could
                      be set to ACTIVE or INACTIVE.
                    type: string
                  systemTags:
                    additionalProperties:
                      type: string
                    description: 'These are system tags for this resource. Each key
                      is predefined and scoped to a namespace.  Example: {"orcl-cloud.free-tier-retained":
                      "true"}'
                    type: object
                    x-kubernetes-map-type: granular
                  timeCreated:
                    description: 'This is the date and time the schedule was created,
                      in the format defined by RFC 3339.  Example: 2016-08-25T21:10:29.600Z'
                    type: string
                  timeEnds:
                    description: '(Updatable) This is the date and time the schedule
                      ends, in the format defined by RFC 3339  Example: 2016-08-25T21:10:29.600Z'
                    type: string
                  timeLastRun:
                    description: 'This is the date and time the schedule runs last
                      time, in the format defined by RFC 3339.  Example: 2016-08-25T21:10:29.600Z'
                    type: string
                  timeNextRun:
                    description: 'This is the date and time the schedule run the next
                      time, in the format defined by RFC 3339.  Example: 2016-08-25T21:10:29.600Z'
                    type: string
                  timeStarts:
                    description: '(Updatable) This is the date and time the schedule
                      starts, in the format defined by RFC 3339  Example: 2016-08-25T21:10:29.600Z'
                    type: string
                  timeUpdated:
                    description: 'This is the date and time the schedule was updated,
                      in the format defined by RFC 3339.  Example: 2016-08-25T21:10:29.600Z'
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
