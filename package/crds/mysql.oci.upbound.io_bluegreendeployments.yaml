---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: bluegreendeployments.mysql.oci.upbound.io
spec:
  group: mysql.oci.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - oci
    kind: BlueGreenDeployment
    listKind: BlueGreenDeploymentList
    plural: bluegreendeployments
    singular: bluegreendeployment
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BlueGreenDeployment is the Schema for the BlueGreenDeployments
          API. Provides the Blue Green Deployment resource in Oracle Cloud Infrastructure
          MySQL Database service
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BlueGreenDeploymentSpec defines the desired state of BlueGreenDeployment
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  channelDetails:
                    description: Replication channel details for a blue/green deployment.
                    items:
                      properties:
                        applierUsername:
                          description: The username for the replication applier of
                            the target MySQL DB System.
                          type: string
                        sourcePasswordSecretRef:
                          description: The password for the source DB system user
                            used by the blue/green workflow to configure the replication
                            channel. The password must be between 8 and 32 characters
                            long, and must contain at least 1 numeric character, 1
                            lowercase character, 1 uppercase character, and 1 special
                            (nonalphanumeric) character.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        sourceUsername:
                          description: The username on the source DB system used by
                            the blue/green workflow to configure the replication channel.
                            The username has a maximum length of 96 characters. For
                            more information, please see the MySQL documentation
                          type: string
                        sslCaCertificate:
                          description: The CA certificate of the server used for VERIFY_IDENTITY
                            and VERIFY_CA ssl modes.
                          items:
                            properties:
                              certificateType:
                                description: The type of CA certificate.
                                type: string
                              contents:
                                description: The string containing the CA certificate
                                  in PEM format.
                                type: string
                            type: object
                          type: array
                        sslMode:
                          description: The SSL mode of the replication channel created
                            by the blue/green workflow. VERIFY_CA and VERIFY_IDENTITY
                            require sslCaCertificate. REQUIRED and DISABLED must not
                            include sslCaCertificate.
                          type: string
                      type: object
                    type: array
                  compartmentId:
                    description: (Updatable) The OCID of the compartment.
                    type: string
                  compartmentIdRef:
                    description: Reference to a Compartment in identity to populate
                      compartmentId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  compartmentIdSelector:
                    description: Selector for a Compartment in identity to populate
                      compartmentId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Defined tags for this resource. Each
                      key is predefined and scoped to a namespace. Example: {"foo-namespace.bar-key":
                      "value"}'
                    type: object
                    x-kubernetes-map-type: granular
                  deleteTargetDbSystemOnDelete:
                    description: |-
                      (Updatable) Whether to delete the target DB System when this Blue/Green deployment is destroyed. The service deletes the associated replication channel as part of deleting the target DB System. Defaults to false.
                      Whether to delete the target DB System when this Blue/Green deployment is deleted.
                    type: boolean
                  displayName:
                    description: (Updatable) The display name of the blue/green deployment.
                    type: string
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Simple key-value pair that is applied
                      without any predefined name, type or scope. Exists for cross-compatibility
                      only. Example: {"bar-key": "value"}'
                    type: object
                    x-kubernetes-map-type: granular
                  sourceDbSystemId:
                    description: Source DB system OCID.
                    type: string
                  sourceDbSystemIdRef:
                    description: Reference to a MysqlDbSystem in mysql to populate
                      sourceDbSystemId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  sourceDbSystemIdSelector:
                    description: Selector for a MysqlDbSystem in mysql to populate
                      sourceDbSystemId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  switchoverTrigger:
                    description: (Updatable) An optional property when incremented
                      triggers Switchover. Could be set to any integer value.
                    type: number
                  targetDbSystemDetails:
                    description: Target DB System overrides for a blue/green deployment.
                    items:
                      properties:
                        configurationId:
                          description: The OCID of the configuration to apply to the
                            target DB System. If omitted, the target DB System inherits
                            the source DB System configuration.
                          type: string
                        configurationIdRef:
                          description: Reference to a MysqlConfiguration in mysql
                            to populate configurationId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        configurationIdSelector:
                          description: Selector for a MysqlConfiguration in mysql
                            to populate configurationId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        dataStorageSizeInGb:
                          description: Initial data storage size in GiBs for the target
                            DB System. If omitted, the target DB System uses the source
                            DB System storage size.
                          type: number
                        mysqlVersion:
                          description: Target MySQL engine version.
                          type: string
                        shapeName:
                          description: The shape of the target DB System. The shape
                            determines resources allocated to the DB System - CPU
                            cores and memory for VM shapes; CPU cores, memory and
                            storage for non-VM (or bare metal) shapes. To get a list
                            of shapes, use the ListShapes operation.
                          type: string
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  channelDetails:
                    description: Replication channel details for a blue/green deployment.
                    items:
                      properties:
                        applierUsername:
                          description: The username for the replication applier of
                            the target MySQL DB System.
                          type: string
                        sourcePasswordSecretRef:
                          description: The password for the source DB system user
                            used by the blue/green workflow to configure the replication
                            channel. The password must be between 8 and 32 characters
                            long, and must contain at least 1 numeric character, 1
                            lowercase character, 1 uppercase character, and 1 special
                            (nonalphanumeric) character.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        sourceUsername:
                          description: The username on the source DB system used by
                            the blue/green workflow to configure the replication channel.
                            The username has a maximum length of 96 characters. For
                            more information, please see the MySQL documentation
                          type: string
                        sslCaCertificate:
                          description: The CA certificate of the server used for VERIFY_IDENTITY
                            and VERIFY_CA ssl modes.
                          items:
                            properties:
                              certificateType:
                                description: The type of CA certificate.
                                type: string
                              contents:
                                description: The string containing the CA certificate
                                  in PEM format.
                                type: string
                            type: object
                          type: array
                        sslMode:
                          description: The SSL mode of the replication channel created
                            by the blue/green workflow. VERIFY_CA and VERIFY_IDENTITY
                            require sslCaCertificate. REQUIRED and DISABLED must not
                            include sslCaCertificate.
                          type: string
                      required:
                      - sourcePasswordSecretRef
                      type: object
                    type: array
                  compartmentId:
                    description: (Updatable) The OCID of the compartment.
                    type: string
                  compartmentIdRef:
                    description: Reference to a Compartment in identity to populate
                      compartmentId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  compartmentIdSelector:
                    description: Selector for a Compartment in identity to populate
                      compartmentId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Defined tags for this resource. Each
                      key is predefined and scoped to a namespace. Example: {"foo-namespace.bar-key":
                      "value"}'
                    type: object
                    x-kubernetes-map-type: granular
                  deleteTargetDbSystemOnDelete:
                    description: |-
                      (Updatable) Whether to delete the target DB System when this Blue/Green deployment is destroyed. The service deletes the associated replication channel as part of deleting the target DB System. Defaults to false.
                      Whether to delete the target DB System when this Blue/Green deployment is deleted.
                    type: boolean
                  displayName:
                    description: (Updatable) The display name of the blue/green deployment.
                    type: string
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Simple key-value pair that is applied
                      without any predefined name, type or scope. Exists for cross-compatibility
                      only. Example: {"bar-key": "value"}'
                    type: object
                    x-kubernetes-map-type: granular
                  sourceDbSystemId:
                    description: Source DB system OCID.
                    type: string
                  sourceDbSystemIdRef:
                    description: Reference to a MysqlDbSystem in mysql to populate
                      sourceDbSystemId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  sourceDbSystemIdSelector:
                    description: Selector for a MysqlDbSystem in mysql to populate
                      sourceDbSystemId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  switchoverTrigger:
                    description: (Updatable) An optional property when incremented
                      triggers Switchover. Could be set to any integer value.
                    type: number
                  targetDbSystemDetails:
                    description: Target DB System overrides for a blue/green deployment.
                    items:
                      properties:
                        configurationId:
                          description: The OCID of the configuration to apply to the
                            target DB System. If omitted, the target DB System inherits
                            the source DB System configuration.
                          type: string
                        configurationIdRef:
                          description: Reference to a MysqlConfiguration in mysql
                            to populate configurationId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        configurationIdSelector:
                          description: Selector for a MysqlConfiguration in mysql
                            to populate configurationId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        dataStorageSizeInGb:
                          description: Initial data storage size in GiBs for the target
                            DB System. If omitted, the target DB System uses the source
                            DB System storage size.
                          type: number
                        mysqlVersion:
                          description: Target MySQL engine version.
                          type: string
                        shapeName:
                          description: The shape of the target DB System. The shape
                            determines resources allocated to the DB System - CPU
                            cores and memory for VM shapes; CPU cores, memory and
                            storage for non-VM (or bare metal) shapes. To get a list
                            of shapes, use the ListShapes operation.
                          type: string
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  - MustCreate
                  - Orphan
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.channelDetails is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.channelDetails)
                || (has(self.initProvider) && has(self.initProvider.channelDetails))'
            - message: spec.forProvider.displayName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.displayName)
                || (has(self.initProvider) && has(self.initProvider.displayName))'
            - message: spec.forProvider.targetDbSystemDetails is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.targetDbSystemDetails)
                || (has(self.initProvider) && has(self.initProvider.targetDbSystemDetails))'
          status:
            description: BlueGreenDeploymentStatus defines the observed state of BlueGreenDeployment.
            properties:
              atProvider:
                properties:
                  activeDbSystemId:
                    description: The DB system OCID that currently owns the client-facing
                      VIP and serves traffic.
                    type: string
                  channelDetails:
                    description: Replication channel details for a blue/green deployment.
                    items:
                      properties:
                        applierUsername:
                          description: The username for the replication applier of
                            the target MySQL DB System.
                          type: string
                        sourceUsername:
                          description: The username on the source DB system used by
                            the blue/green workflow to configure the replication channel.
                            The username has a maximum length of 96 characters. For
                            more information, please see the MySQL documentation
                          type: string
                        sslCaCertificate:
                          description: The CA certificate of the server used for VERIFY_IDENTITY
                            and VERIFY_CA ssl modes.
                          items:
                            properties:
                              certificateType:
                                description: The type of CA certificate.
                                type: string
                              contents:
                                description: The string containing the CA certificate
                                  in PEM format.
                                type: string
                            type: object
                          type: array
                        sslMode:
                          description: The SSL mode of the replication channel created
                            by the blue/green workflow. VERIFY_CA and VERIFY_IDENTITY
                            require sslCaCertificate. REQUIRED and DISABLED must not
                            include sslCaCertificate.
                          type: string
                      type: object
                    type: array
                  compartmentId:
                    description: (Updatable) The OCID of the compartment.
                    type: string
                  definedTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Defined tags for this resource. Each
                      key is predefined and scoped to a namespace. Example: {"foo-namespace.bar-key":
                      "value"}'
                    type: object
                    x-kubernetes-map-type: granular
                  deleteTargetDbSystemOnDelete:
                    description: |-
                      (Updatable) Whether to delete the target DB System when this Blue/Green deployment is destroyed. The service deletes the associated replication channel as part of deleting the target DB System. Defaults to false.
                      Whether to delete the target DB System when this Blue/Green deployment is deleted.
                    type: boolean
                  displayName:
                    description: (Updatable) The display name of the blue/green deployment.
                    type: string
                  freeformTags:
                    additionalProperties:
                      type: string
                    description: '(Updatable) Simple key-value pair that is applied
                      without any predefined name, type or scope. Exists for cross-compatibility
                      only. Example: {"bar-key": "value"}'
                    type: object
                    x-kubernetes-map-type: granular
                  id:
                    description: The OCID of the blue/green deployment.
                    type: string
                  lifecycleDetails:
                    description: Additional lifecycle details.
                    type: string
                  replicationChannelId:
                    description: Replication channel OCID.
                    type: string
                  sourceDbSystemId:
                    description: Source DB system OCID.
                    type: string
                  sslMode:
                    description: The SSL mode of the replication channel created by
                      the blue/green workflow. VERIFY_CA and VERIFY_IDENTITY require
                      sslCaCertificate. REQUIRED and DISABLED must not include sslCaCertificate.
                    type: string
                  state:
                    description: The current lifecycle state.
                    type: string
                  switchoverStatus:
                    description: Stage of the most recent switchover workflow. SWITCHOVER_FAILED
                      indicates terminal switchover failure.
                    type: string
                  switchoverTrigger:
                    description: (Updatable) An optional property when incremented
                      triggers Switchover. Could be set to any integer value.
                    type: number
                  systemTags:
                    additionalProperties:
                      type: string
                    description: 'Usage of system tag keys. These predefined keys
                      are scoped to namespaces. Example: {"orcl-cloud.free-tier-retained":
                      "true"}'
                    type: object
                    x-kubernetes-map-type: granular
                  targetDbSystemDetails:
                    description: Target DB System overrides for a blue/green deployment.
                    items:
                      properties:
                        configurationId:
                          description: The OCID of the configuration to apply to the
                            target DB System. If omitted, the target DB System inherits
                            the source DB System configuration.
                          type: string
                        dataStorageSizeInGb:
                          description: Initial data storage size in GiBs for the target
                            DB System. If omitted, the target DB System uses the source
                            DB System storage size.
                          type: number
                        mysqlVersion:
                          description: Target MySQL engine version.
                          type: string
                        shapeName:
                          description: The shape of the target DB System. The shape
                            determines resources allocated to the DB System - CPU
                            cores and memory for VM shapes; CPU cores, memory and
                            storage for non-VM (or bare metal) shapes. To get a list
                            of shapes, use the ListShapes operation.
                          type: string
                      type: object
                    type: array
                  targetDbSystemId:
                    description: Green/target DB system OCID for the deployment pair.
                    type: string
                  timeCreated:
                    description: The time the deployment was created.
                    type: string
                  timeUpdated:
                    description: The time the deployment was last updated.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
